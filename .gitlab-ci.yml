image: node:20

pages:
  stage: deploy
  script:
    - npm ci
    - npm run build
    - rm -rf public
    - mkdir public
    - cp index.html public/
    - cp -r dist public/dist
    - cp -r images public/images
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
